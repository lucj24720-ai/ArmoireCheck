#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for backend
cd backend && npx lint-staged

# Run lint-staged for frontend (if configured)
cd ../frontend && npx lint-staged || true

echo "✅ Pre-commit checks passed!"
